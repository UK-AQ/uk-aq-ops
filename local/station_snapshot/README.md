# Local Station Snapshot Backend

This directory holds the migrated station snapshot local backend API.

Primary file:

- `server/uk_aq_station_snapshot_local.py`

Run locally from repo root:

```bash
local/scripts/run_station_snapshot_local.sh
```

The frontend served by this backend is `station_snapshot/index.html`.
